# Specification for XpringKit's XCode project.
name: XpringKit
options:
  bundleIdPrefix: io.xpring
targets:
  XpringKit:
    type: framework
    platform: iOS
    sources: [XpringKit]
    scheme:
      testTargets:
        - XpringKitTests
      gatherCoverageData: true
    preBuildScripts:
      - script: |
                mkdir -p ./XpringKit/generated
                protoc --proto_path=./terram-protos/models --swift_out=./XpringKit/generated --swiftgrpc_out=./XpringKit/generated ./terram-protos/models/*.proto
        name: Build Protos
    postCompileScripts:
      - script: swiftlint --config .swiftlint.yml
        name: SwiftLint
    dependencies:
      - carthage: SwiftProtobuf
  XpringKitTests:
    type: bundle.unit-test
    platform: iOS
    sources: [Tests]
    dependencies:
      - target: XpringKit
